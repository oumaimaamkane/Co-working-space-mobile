import 'package:flutter/material.dart';

class DashminScreen extends StatelessWidget {
  const DashminScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Welcome to Admin Dashboard'),
    );
  }
}
