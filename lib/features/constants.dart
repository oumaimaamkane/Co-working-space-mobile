import 'package:flutter/material.dart';

final buttonStyle = ElevatedButton.styleFrom(
    backgroundColor: const Color(0xFF55bbae), // Background color
    foregroundColor: Colors.white, // Text color
  );

  